/** Zalo defaults — single source; override via NEXT_PUBLIC_ZALO_CONTACT_URL / ZALO_* env. */
export const DEFAULT_ZALO_PHONE = "0834513456";
export const DEFAULT_ZALO_URL = `https://zalo.me/${DEFAULT_ZALO_PHONE}`;
